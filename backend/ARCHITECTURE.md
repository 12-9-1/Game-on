# Arquitectura del Backend Node.js

## 🏗️ Diagrama General

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend React                            │
│              (http://localhost:5173)                         │
└────────────────────┬────────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │                         │
        ▼                         ▼
   HTTP Routes              Socket.IO Events
   (Express)                (Real-time)
        │                         │
        └────────────┬────────────┘
                     │
┌────────────────────▼────────────────────────────────────────┐
│              Backend Node.js (Port 5000)                     │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Express Server + Socket.IO                          │  │
│  │  ├─ CORS Configuration                               │  │
│  │  ├─ JWT Middleware                                   │  │
│  │  └─ Error Handling                                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Routes (HTTP)                                       │  │
│  │  ├─ POST /register                                   │  │
│  │  ├─ POST /login                                      │  │
│  │  └─ GET /protected                                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Socket.IO Events                                    │  │
│  │  ├─ Lobby Management                                 │  │
│  │  ├─ Game Logic                                       │  │
│  │  ├─ Chat                                             │  │
│  │  └─ Real-time Updates                                │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  Services                                            │  │
│  │  └─ AI Service (Question Generation)                 │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  In-Memory Storage                                   │  │
│  │  ├─ lobbies                                          │  │
│  │  ├─ userLobbies                                      │  │
│  │  ├─ activeQuestions                                  │  │
│  │  ├─ playerAnswers                                    │  │
│  │  ├─ questionQueue                                    │  │
│  │  └─ questionTimers                                   │  │
│  └──────────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────────┘
                     │
                     ▼
        ┌────────────────────────────┐
        │   MongoDB Database         │
        │ (Users + Persistence)      │
        └────────────────────────────┘
```

## 📊 Flujo de Datos

### 1. Autenticación

```
┌─────────────────┐
│  Frontend       │
│  (Registro)     │
└────────┬────────┘
         │
         │ POST /register
         │ { name, email, password }
         ▼
┌─────────────────────────────────────┐
│  Express Route (auth.js)            │
│  ├─ Validar entrada                 │
│  ├─ Verificar email único           │
│  ├─ Hashear contraseña (bcryptjs)   │
│  └─ Crear usuario en MongoDB        │
└────────┬────────────────────────────┘
         │
         │ Generar JWT
         │
         ▼
┌─────────────────────────────────────┐
│  Response                           │
│  {                                  │
│    token: "jwt_token",              │
│    user: { public_id, name, email } │
│  }                                  │
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────┐
│  Frontend       │
│  localStorage   │
│  .setItem(token)│
└─────────────────┘
```

### 2. Crear Lobby

```
┌──────────────────────┐
│  Frontend            │
│  socket.emit(        │
│    'create_lobby',   │
│    { player_name }   │
│  )                   │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Socket Handler (socketHandler.js)       │
│  handle_create_lobby()                   │
│  ├─ Generar ID corto                     │
│  ├─ Crear lobby en memoria               │
│  ├─ Agregar jugador                      │
│  ├─ Unir socket a room                   │
│  └─ Guardar en userLobbies               │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  In-Memory Storage                       │
│  lobbies[lobby_id] = {                   │
│    id, host, players, status, ...        │
│  }                                       │
│  userLobbies[socket_id] = lobby_id       │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Frontend                                │
│  socket.on('lobby_created', (data) => {  │
│    // Mostrar lobby creado               │
│  })                                      │
└──────────────────────────────────────────┘
```

### 3. Iniciar Juego

```
┌──────────────────────┐
│  Host (Frontend)     │
│  socket.emit(        │
│    'start_game'      │
│  )                   │
└──────────┬───────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Socket Handler                          │
│  handle_start_game()                     │
│  ├─ Verificar permisos (es host)         │
│  ├─ Verificar todos ready                │
│  ├─ Cambiar status a 'playing'           │
│  ├─ Inicializar scores                   │
│  └─ Generar primera pregunta             │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  AI Service (aiService.js)               │
│  generateSingleQuestionSync()            │
│  ├─ Llamar Open Trivia DB API            │
│  ├─ Decodificar HTML entities            │
│  ├─ Mezclar opciones                     │
│  └─ Retornar pregunta                    │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Socket Handler                          │
│  sendNextQuestion()                      │
│  ├─ Preparar pregunta (sin respuesta)    │
│  ├─ Inicializar playerAnswers            │
│  ├─ Emitir 'new_question'                │
│  └─ Iniciar temporizador (32s)           │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Todos los Jugadores (Frontend)          │
│  socket.on('new_question', (data) => {   │
│    // Mostrar pregunta y opciones        │
│    // Iniciar timer de 30s               │
│  })                                      │
└──────────────────────────────────────────┘
```

### 4. Enviar Respuesta

```
┌──────────────────────────────┐
│  Jugador (Frontend)          │
│  socket.emit(                │
│    'submit_answer',          │
│    { answer_index: 0 }       │
│  )                           │
└──────────┬────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│  Socket Handler                          │
│  handle_submit_answer()                  │
│  ├─ Verificar que está en lobby          │
│  ├─ Verificar que no respondió antes     │
│  ├─ Calcular si es correcta              │
│  ├─ Calcular puntos (base + bonus)       │
│  ├─ Actualizar score del jugador         │
│  └─ Guardar en playerAnswers             │
└──────────┬───────────────────────────────┘
           │
           ├─────────────────────────────────┐
           │                                 │
           ▼                                 ▼
    ┌─────────────────┐           ┌──────────────────┐
    │ Emitir al       │           │ Emitir a todos   │
    │ jugador:        │           │ en el lobby:     │
    │ 'answer_result' │           │ 'player_answered'│
    │ 'lobby_updated' │           │ 'lobby_updated'  │
    └─────────────────┘           └──────────────────┘
           │                                 │
           └─────────────────┬───────────────┘
                             │
                             ▼
                    ┌──────────────────────┐
                    │ ¿Todos respondieron? │
                    └──────────┬───────────┘
                               │
                    ┌──────────┴──────────┐
                    │                     │
                   Sí                     No
                    │                     │
                    ▼                     ▼
            ┌──────────────┐      ┌─────────────┐
            │ Siguiente    │      │ Esperar     │
            │ pregunta     │      │ temporizador│
            └──────────────┘      └─────────────┘
```

## 🔄 Ciclo de Vida de un Lobby

```
┌─────────────────────────────────────────────────────────────┐
│                    WAITING (Esperando)                       │
│  - Jugadores pueden unirse                                   │
│  - Host puede iniciar juego                                  │
│  - Jugadores pueden marcar ready                             │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ Host inicia juego
                       │ (todos ready)
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                    PLAYING (Jugando)                         │
│  - Preguntas se envían                                       │
│  - Jugadores responden                                       │
│  - Puntos se calculan                                        │
│  - Temporizadores activos                                    │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ ¿Alguien ganó?
                       │ (llegó a 10,000 puntos)
                       ▼
┌─────────────────────────────────────────────────────────────┐
│               ROUND_FINISHED (Ronda Terminada)               │
│  - Resultados mostrados                                      │
│  - Rankings visibles                                         │
│  - Host puede solicitar nueva ronda                          │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ Host solicita nueva ronda
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│            WAITING_NEW_ROUND (Esperando Nueva Ronda)         │
│  - Jugadores marcan ready                                    │
│  - Scores se resetean                                        │
│  - Nuevas preguntas se generan                               │
└──────────────────────┬──────────────────────────────────────┘
                       │
                       │ Todos ready
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                    PLAYING (Jugando)                         │
│  - Nueva ronda comienza                                      │
│  - Mismo ciclo que antes                                     │
└─────────────────────────────────────────────────────────────┘
```

## 📦 Estructura de Carpetas

```
server/
├── src/
│   ├── index.js
│   │   └─ Inicializa Express, Socket.IO, MongoDB
│   │   └─ Registra rutas y eventos
│   │
│   ├── config/
│   │   └── database.js
│   │       └─ Conexión a MongoDB
│   │
│   ├── models/
│   │   └── User.js
│   │       └─ Esquema de usuario (Mongoose)
│   │
│   ├── routes/
│   │   └── auth.js
│   │       ├─ POST /register
│   │       ├─ POST /login
│   │       └─ GET /protected
│   │
│   ├── middleware/
│   │   └── auth.js
│   │       └─ tokenRequired (JWT verification)
│   │
│   ├── services/
│   │   └── aiService.js
│   │       ├─ getQuestionFromOpenTDB()
│   │       ├─ generateSingleQuestionSync()
│   │       └─ generateRoundQuestions()
│   │
│   └── sockets/
│       └── socketHandler.js
│           ├─ registerSocketEvents()
│           ├─ handle_create_lobby()
│           ├─ handle_join_lobby()
│           ├─ handle_start_game()
│           ├─ handle_submit_answer()
│           ├─ sendNextQuestion()
│           ├─ endGame()
│           └─ ... (más eventos)
│
├── package.json
├── .env.example
└── README.md
```

## 🔐 Seguridad

```
┌─────────────────────────────────────────────────────────────┐
│                    CORS Configuration                        │
│  - Origin: http://localhost:5173                             │
│  - Methods: GET, POST, PUT, DELETE, OPTIONS                 │
│  - Credentials: true                                         │
│  - Headers: Content-Type, Authorization, x-access-token     │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                  JWT Authentication                          │
│  - Secret: JWT_SECRET (from .env)                            │
│  - Expiration: 1 day                                         │
│  - Header: x-access-token                                    │
│  - Payload: { public_id, exp }                               │
└─────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│              Password Hashing (bcryptjs)                     │
│  - Algorithm: bcrypt                                         │
│  - Salt rounds: 10                                           │
│  - Stored in MongoDB                                         │
└─────────────────────────────────────────────────────────────┘
```

## 🚀 Escalabilidad Futura

```
Actual (Single Server):
┌──────────────────┐
│  Node.js Server  │
│  (In-Memory)     │
└──────────────────┘

Futura (Distributed):
┌──────────────────┐     ┌──────────────────┐
│  Node.js Server 1│     │  Node.js Server 2│
└────────┬─────────┘     └────────┬─────────┘
         │                        │
         └────────────┬───────────┘
                      │
                      ▼
            ┌──────────────────────┐
            │  Redis (Shared State)│
            │  (Lobbies, Messages) │
            └──────────────────────┘
                      │
                      ▼
            ┌──────────────────────┐
            │  MongoDB (Persistence)
            │  (Users, History)    │
            └──────────────────────┘
```

## 📊 Dependencias

```
express (HTTP Server)
    │
    ├─ socket.io (Real-time)
    │
    ├─ cors (Cross-Origin)
    │
    ├─ mongoose (MongoDB ODM)
    │   └─ mongodb (Driver)
    │
    ├─ jsonwebtoken (JWT)
    │
    ├─ bcryptjs (Password Hashing)
    │
    ├─ uuid (ID Generation)
    │
    ├─ axios (HTTP Client)
    │   └─ Open Trivia DB API
    │
    └─ dotenv (Environment Variables)
```

## 🎯 Flujo Completo de Juego

```
1. REGISTRO/LOGIN
   └─ Usuario se autentica
   └─ Recibe JWT token

2. CREAR/UNIRSE A LOBBY
   └─ Usuario crea o se une a lobby
   └─ Espera a otros jugadores
   └─ Marca ready

3. INICIAR JUEGO
   └─ Host inicia (todos ready)
   └─ Servidor genera preguntas
   └─ Primera pregunta se envía

4. JUGAR
   └─ Jugadores responden preguntas
   └─ Puntos se calculan
   └─ Siguiente pregunta se envía
   └─ Temporizador avanza automáticamente

5. FIN DE RONDA
   └─ Alguien alcanza 10,000 puntos
   └─ Resultados se muestran
   └─ Rankings se actualizan

6. NUEVA RONDA O LOBBY
   └─ Host solicita nueva ronda
   └─ Scores se resetean
   └─ Vuelve a paso 4
   └─ O vuelven al lobby (paso 2)
```

¡Arquitectura lista! 🏗️
